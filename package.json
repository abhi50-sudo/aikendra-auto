{
  "name": "ai-kendra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/fetchData.js && vite build && node scripts/generateStatic.js",
    "preview": "npm run build && wrangler dev",
    "scrape": "python scripts/run_daily_pipeline.py",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.31.2",
    "csv-parse": "^6.2.1",
    "vite": "^8.0.0",
    "wrangler": "^4.81.1"
  }
}
